from setuptools import setup, find_packages

setup(
    name='sirius',
    version='0.0.1',
    description='just a simplesirius',

    author='jj',

    packages=find_packages(),
    platforms='any'
)
